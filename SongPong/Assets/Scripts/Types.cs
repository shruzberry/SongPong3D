using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Types
{
    public enum Direction
    {
        positive, negative
    }

    public enum BallTypes
    {
        simple, bounce
    }

    public enum Paddles
    {
        P1, P2
    }

    public enum Axis
    {
        x, y
    }
}
